import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Account - Institute OS",
  description: "Manage your Institute OS account",
};

const page = () => {
  return <></>;
};

export default page;
